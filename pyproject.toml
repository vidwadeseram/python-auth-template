[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-auth-template"
version = "0.1.0"
description = "FastAPI authentication template"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]
